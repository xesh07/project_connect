export default function DiscoverPage() {
  return (
    <div className="shell">
      <section className="panel p-8">
        <h1 className="font-mono text-3xl font-semibold">Discover Talent</h1>
        <p className="mt-2 text-[color:var(--muted-ink)]">
          Phase 3: search and filters by category, city, skill tags, and paid or
          unpaid preference.
        </p>
      </section>
    </div>
  );
}
